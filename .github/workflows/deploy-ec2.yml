name: Deploy to EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script_stop: true
          command_timeout: 45m
          script: |
            set -e
            cd ~/linex
            git fetch origin
            git reset --hard origin/main
            npm install
            export NODE_OPTIONS="--max-old-space-size=2048"
            npx prisma migrate deploy
            # Stop app and clear .next so `next build` is not blocked on .next/lock
            # (stale lock or concurrent use from a previous failed/partial build).
            pm2 stop linex || true
            rm -f .next/lock
            rm -rf .next
            npm run build
            pm2 restart linex --update-env
            pm2 save 